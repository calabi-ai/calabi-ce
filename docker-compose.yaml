# Calabi Community Edition v1.0
#
# Start:   docker compose up -d
# Stop:    docker compose down
# Reset:   docker compose down -v
# Status:  docker compose ps
# Logs:    docker compose logs -f
#
# Access:  http://localhost:8080
# Login:   ce-admin@calabi.dev / Calabi@CE2025!
#
# Requirements: Docker 24+, 4 CPU, 8GB RAM

x-common: &common
  restart: unless-stopped
  networks:
    - calabi-net

services:
  # PostgreSQL
  postgres:
    <<: *common
    image: postgres:16-alpine
    container_name: calabi-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-calabi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-calabi_ce_2025}
      POSTGRES_DB: calabi_catalogue
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-calabi}"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

  # OpenSearch
  opensearch:
    <<: *common
    image: opensearchproject/opensearch:2.17.0
    container_name: calabi-opensearch
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      - cluster.name=calabi-ce
      - bootstrap.memory_lock=true
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=C@labi_OS_2025!
    ulimits:
      memlock: { soft: -1, hard: -1 }
      nofile: { soft: 65536, hard: 65536 }
    volumes:
      - opensearch_data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -qE '(green|yellow)'"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1.5G

  # Catalogue — Database migrations (one-shot)
  calabi-catalogue-init:
    image: ${CALABI_CATALOGUE_IMAGE:-public.ecr.aws/calabi/calabi-catalogue:1.0.0}
    restart: "no"
    networks: [calabi-net]
    container_name: calabi-catalogue-init
    depends_on:
      postgres: { condition: service_healthy }
      opensearch: { condition: service_healthy }
    entrypoint: ["/bin/bash"]
    command:
      - -c
      - |
        echo "Running database migrations..."
        ./bootstrap/openmetadata-ops.sh migrate
        echo "Running reindex..."
        ./bootstrap/openmetadata-ops.sh reindex || true
        echo "Init complete."
    environment:
      DB_DRIVER_CLASS: org.postgresql.Driver
      DB_SCHEME: postgresql
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: ${POSTGRES_USER:-calabi}
      DB_USER_PASSWORD: ${POSTGRES_PASSWORD:-calabi_ce_2025}
      OM_DATABASE: calabi_catalogue
      SEARCH_TYPE: opensearch
      OPENSEARCH_HOST: opensearch
      OPENSEARCH_PORT: "9200"
      OPENSEARCH_SCHEME: http
      ELASTICSEARCH_HOST: opensearch
      ELASTICSEARCH_PORT: "9200"
      ELASTICSEARCH_SCHEME: http

  # Catalogue — Calabi platform UI and API
  calabi-catalogue:
    <<: *common
    image: ${CALABI_CATALOGUE_IMAGE:-public.ecr.aws/calabi/calabi-catalogue:1.0.0}
    container_name: calabi-catalogue
    depends_on:
      postgres: { condition: service_healthy }
      opensearch: { condition: service_healthy }
      calabi-catalogue-init: { condition: service_completed_successfully }
    environment:
      DB_DRIVER_CLASS: org.postgresql.Driver
      DB_SCHEME: postgresql
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: ${POSTGRES_USER:-calabi}
      DB_USER_PASSWORD: ${POSTGRES_PASSWORD:-calabi_ce_2025}
      OM_DATABASE: calabi_catalogue
      SEARCH_TYPE: opensearch
      OPENSEARCH_HOST: opensearch
      OPENSEARCH_PORT: "9200"
      OPENSEARCH_SCHEME: http
      ELASTICSEARCH_HOST: opensearch
      ELASTICSEARCH_PORT: "9200"
      ELASTICSEARCH_SCHEME: http
      SERVER_PORT: "8585"
      SERVER_ADMIN_PORT: "8586"
      AUTHENTICATION_PROVIDER: basic
      AUTHORIZER_ADMIN_PRINCIPALS: "[admin]"
      PIPELINE_SERVICE_CLIENT_ENABLED: "false"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:8585/api/v1/system/version || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 30
      start_period: 600s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1.5G

  # Post-startup setup (one-shot)
  calabi-ce-setup:
    image: alpine:3.19
    restart: "no"
    networks: [calabi-net]
    container_name: calabi-ce-setup
    depends_on:
      calabi-catalogue: { condition: service_healthy }
    entrypoint: ["/bin/sh"]
    command: ["/scripts/ce-setup.sh"]
    volumes:
      - ./scripts/ce-setup.sh:/scripts/ce-setup.sh:ro

  # CalabiIQ BI (Superset)
  calabi-bi:
    <<: *common
    image: ${CALABI_BI_IMAGE:-apache/superset:4.1.1}
    container_name: calabi-bi
    depends_on:
      postgres: { condition: service_healthy }
    environment:
      SUPERSET_SECRET_KEY: ${SUPERSET_SECRET_KEY:-calabi-ce-secret-change-me-in-prod}
      DATABASE_HOST: postgres
      DATABASE_PORT: "5432"
      DATABASE_USER: ${POSTGRES_USER:-calabi}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-calabi_ce_2025}
      DATABASE_DB: calabi_bi
      DATABASE_DIALECT: postgresql
      SUPERSET_LOAD_EXAMPLES: "${SUPERSET_LOAD_EXAMPLES:-no}"
      SUPERSET_CONFIG_PATH: "/app/pythonpath/superset_config.py"
    ports:
      - "${BI_PORT:-8088}:8088"
    volumes:
      - superset_data:/app/superset_home
      - ./config/bi/superset_config.py:/app/pythonpath/superset_config.py:ro
      - ./config/bi/static/calabi-logo.svg:/app/superset/static/assets/calabi-logo.svg:ro
      - ./config/bi/static/calabi-icon.png:/app/superset/static/assets/calabi-icon.png:ro
      - ./config/bi/static/calabi-favicon.svg:/app/superset/static/assets/calabi-favicon.svg:ro
    command: >
      bash -c "
        superset db upgrade &&
        superset fab create-admin --username admin --firstname Calabi --lastname Admin --email admin@calabi.dev --password $${SUPERSET_ADMIN_PASSWORD:-admin} || true &&
        superset init &&
        echo 'Loading sample dashboards...' &&
        superset load-examples --force || true &&
        echo 'Sample data loaded.' &&
        superset run -h 0.0.0.0 -p 8088 --with-threads
      "
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8088/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: 1G

  # License proxy (CE limit enforcement)
  calabi-license-proxy:
    <<: *common
    image: ${CALABI_LICENSE_PROXY_IMAGE:-public.ecr.aws/calabi/calabi-license-proxy:1.0.0}
    container_name: calabi-license-proxy
    environment:
      CALABI_EDITION: "community"
      CE_MAX_CATALOGUE_USERS: "${CE_MAX_CATALOGUE_USERS:-5}"
      CE_MAX_CATALOGUE_ASSETS: "${CE_MAX_CATALOGUE_ASSETS:-500}"
      CATALOGUE_URL: http://calabi-catalogue:8585
      BI_URL: http://calabi-bi:8088
    depends_on:
      calabi-catalogue: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8090/health')\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M

  # Gateway (single entry point)
  calabi-gateway:
    <<: *common
    image: nginx:alpine
    container_name: calabi-gateway
    depends_on:
      calabi-catalogue: { condition: service_healthy }
      calabi-bi: { condition: service_healthy }
      calabi-license-proxy: { condition: service_healthy }
    volumes:
      - ./config/gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "${CALABI_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/api/license/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:
    name: calabi-ce-postgres
  opensearch_data:
    name: calabi-ce-opensearch
  superset_data:
    name: calabi-ce-superset

networks:
  calabi-net:
    name: calabi-ce-network
    driver: bridge
